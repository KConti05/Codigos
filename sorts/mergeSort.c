/*É um método de ordenação que utiliza do "Separar e Conquistar".
Ele divide o vetor em subvetores recursivamente até que cada elemento possua um vetor unitário próprio, então organiza esses subvetores até um vetor
ordenado.*/

#include <stdio.h>

void mergeSort(
